<form method="post" action="{{ path('encadrant_delete', {'id': encadrant.id}) }}" onsubmit="return confirm('Voulez-vous supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ encadrant.id) }}">
    <button class="btn btn-lg btn-dark">Modifier</button>
</form>
