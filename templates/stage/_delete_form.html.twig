<form method="post" action="{{ path('stage_delete', {'id': stage.id}) }}" onsubmit="return confirm('Voulez-vous supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stage.id) }}">
    <button class="btn btn-outline-danger">Supprimer</button>
</form>
