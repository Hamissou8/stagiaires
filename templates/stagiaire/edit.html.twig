{% extends 'base.html.twig' %}

{% block title %}Modifier Stagiaire{% endblock %}

{% block body %}
    <h1>Modifier ce stagiaire</h1>

    {{ include('stagiaire/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a class="btn btn-outline-primary" href="{{ path('stagiaire_index') }}">Liste des stagiaires</a>

    {{ include('stagiaire/_delete_form.html.twig') }}
{% endblock %}
