<form method="post" action="{{ path('absence_delete', {'id': absence.id}) }}" onsubmit="return confirm('Voulez-vous supprimer cette absence?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ absence.id) }}">
    <button class="btn btn-lg btn-danger">Supprimer</button>
</form>
